services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: edgar
      POSTGRES_PASSWORD: edgar
      POSTGRES_DB: edgar_parse
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U edgar -d edgar_parse"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://edgar:edgar@db:5432/edgar_parse
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-dev-secret-key}
      DJANGO_DEBUG: ${DJANGO_DEBUG:-true}
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS:-*}
      # Include common Vite ports if dev server picks an alternate (e.g. 5174 when 5173 is taken)
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:5175,http://127.0.0.1:5175,http://localhost:8080,http://127.0.0.1:8080}
      USER_AGENT_EMAIL: ${USER_AGENT_EMAIL:-}
      EDGAR_DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health/')",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s

  test:
    profiles:
      - ci
    build:
      context: .
      dockerfile: Dockerfile
      target: test
    environment:
      DATABASE_URL: postgresql://edgar:edgar@db:5432/edgar_parse
      PYTHONPATH: /app/src
      DJANGO_SETTINGS_MODULE: config.settings_test
    volumes:
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy
    command:
      [
        "/bin/sh",
        "-c",
        "python src/manage.py migrate --noinput && exec /app/docker-ci.sh",
      ]

  # Vite dev server: browser hits :5173; /api is proxied to Django (see frontend/.env.docker).
  # Run: docker compose --profile dev up   (or from frontend/: npm run docker:dev)
  frontend:
    profiles:
      - dev
    image: node:22-bookworm-slim
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "5173:5173"
    env_file:
      - ./frontend/.env.docker
    command:
      ["sh", "-c", "npm ci && npm run dev -- --host 0.0.0.0 --port 5173"]
    depends_on:
      web:
        condition: service_healthy

  frontend-prod:
    profiles:
      - prod
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "8080:80"
    depends_on:
      - web

volumes:
  postgres_data:
